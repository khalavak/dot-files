# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Set the mod-key
set $mod Mod4

# font for window titles. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Various settings
workspace_auto_back_and_forth yes

#force_display_urgency_hint 500ms
focus_follows_mouse yes

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+Q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run

# change focus
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+h focus left

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+J move down
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right
bindsym $mod+Shift+H move left

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+g split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindcode $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1
bindsym capslock+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+exclam move container to workspace 1
bindsym $mod+Shift+quotedbl move container to workspace 2
bindsym $mod+Shift+numbersign move container to workspace 3
bindsym $mod+Shift+currency move container to workspace 4
bindsym $mod+Shift+percent move container to workspace 5
bindsym $mod+Shift+ampersand move container to workspace 6
bindsym $mod+Shift+slash move container to workspace 7
bindsym $mod+Shift+parenleft move container to workspace 8
bindsym $mod+Shift+parenright move container to workspace 9
bindsym $mod+Shift+equal move container to workspace 10

# Workspaces and screen placement

workspace "1" output LVDS
workspace "2" output LVDS
workspace "3" output DisplayPort-2
workspace "4" output DisplayPort-2
workspace "5" output LVDS
workspace "6" output LVDS
workspace "7" output LVDS
workspace "8" output LVDS
workspace "9" output VGA-0
workspace "10" output VGA-0

# Change border style
bindsym $mod+t border normal
bindsym $mod+y border 1pixel
bindsym $mod+u border none

# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exit

# Custom keybindings

# Lock screen

bindsym $mod+End exec --no-startup-id i3lock -t -c 000000 -i ~/Pictures/Wallpaper/computers_digital_art_1280x800_wallpaper_Wallpaper_1600x1200_www.wall321.com.png

# Make the currently focused window a scratchpad
 bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show

# Screen layout configs
bindsym $mod+F4 exec --no-startup-id /home/khalavak/bin/confscreens.py -s
bindcode 235 exec --no-startup-id /home/khalavak/bin/confscreens.py
bindsym $mod+Mod1+1 exec --no-startup-id /home/khalavak/bin/confscreens.py --one
bindsym $mod+Mod1+2 exec --no-startup-id /home/khalavak/bin/confscreens.py -2
bindsym $mod+Mod1+3 exec --no-startup-id /home/khalavak/bin/confscreens.py -3
bindsym $mod+Mod1+4 exec --no-startup-id /home/khalavak/bin/confscreens.py -4
bindsym $mod+Mod1+5 exec --no-startup-id /home/khalavak/bin/confscreens.py -5
bindsym $mod+Mod1+6 exec --no-startup-id /home/khalavak/bin/confscreens.py -6


# Audio volume
bindsym XF86AudioRaiseVolume exec amixer -q sset Master 5%+ unmute
bindsym XF86AudioLowerVolume  exec amixer -q sset Master 5%- unmute
bindsym XF86AudioMute exec amixer -q sset Master toggle

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        

# Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 10 px or 10 ppt
        bindsym 116 resize grow height 10 px or 10 ppt
        bindsym 111 resize shrink height 10 px or 10 ppt
        bindsym 114 resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	position bottom
#        mode hide
        #status_command py3status -c ~/.i3/i3status.conf -i ~/.i3/py3status/
        status_command i3status
	tray_output LVDS
}


# Assign applications to workspaces
assign [class="^urxvt"] 1
assign [class="Thunderbird"] 2
assign [class="Chromium"] 3
assign [class="Pidgin"] 5
assign [class="pidgin"] 5
assign [class="VirtualBox"] 9
assign [title="pinpoint"] 10
assign [class="Firefox"] 4

# Assign modes to applications
# Run pinpoint presentation without borders
for_window [title="pinpoint"] border none

# Start pinpoint with no border
#
# enable floating mode for all XTerm windows
#for_window [class="XTerm"] floating enable

# Make all urxvts use a 1-pixel border:
for_window [title="urxvt"] border normal

# A less useful, but rather funny example:
# makes the window floating as soon as I change
# directory to ~/work
# for_window [title="x200: ~/work"] floating enable

# Execute applications
exec --no-startup-id feh -z --bg-scale ~/Pictures/Wallpaper/computers_digital_art_1280x800_wallpaper_Wallpaper_1600x1200_www.wall321.com.png
# exec --no-startup-id feh -z --bg-scale ~/Pictures/Wallpaper/*
exec --no-startup-id nm-applet
exec --no-startup-id xfce4-power-manager
exec --no-startup-id /opt/davmail/davmail.sh
exec --no-startup-id pidgin
exec --no-startup-id parcellite
exec --no-startup-id xautolock -time 5 -locker 'i3lock -t -c 000000 -i ~/Pictures/Wallpaper/computers_digital_art_1280x800_wallpaper_Wallpaper_1600x1200_www.wall321.com.png' 

